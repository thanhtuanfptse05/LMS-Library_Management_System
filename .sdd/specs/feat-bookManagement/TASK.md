# TASKS.md — Task Breakdown Quản lý Sách

| ID | Task | Files liên quan | Est | Deps | DoD / Spec Refs |
|---|---|---|---|---|---|
| **T-F4-01** | Cập nhật schema BookManagement | `database/LMS_Library_Management_System.sql` | 2h | None | Thêm CHECK constraint cho Book/BookCopy, unique Category.name, bảng `BookImportBatch`, `BookImportError`, index phục vụ danh mục và cảnh báo lệch kho. Refs: FR-F4-17, FR-F4-19..21A. |
| **T-F4-02** | Tạo model/DTO/Exception cho F4 | `Book.java`, `BookCopy.java`, `Category.java`, `Tag.java`, `BookImportBatch.java`, `BookImportError.java`, `BookImportRowDTO.java`, `InventoryMismatchDTO.java`, `ValidationException.java`, `DatabaseException.java` | 3h | T-F4-01 | Java Beans/DTO thuần, không ORM. DTO import biểu diễn sheet `Books`/`BookCopies`, lỗi theo sheet/dòng/cột. |
| **T-F4-03** | Tạo DAO nền cho Book/BookCopy/metadata | `BookDAO.java`, `BookCopyDAO.java`, `CategoryDAO.java`, `TagDAO.java` | 4h | T-F4-02 | 100% PreparedStatement. DAO có overload nhận `Connection`. Không sửa trực tiếp ISBN, Barcode, `totalQuantity`, `availableQuantity`. Refs: FR-F4-01..09, FR-F4-14A. |
| **T-F4-04** | Tạo DAO Audit và lịch sử import | `AuditLogDAO.java`, `BookImportDAO.java` | 3h | T-F4-02 | Ghi `AuditLogs`, `BookImportBatch`, `BookImportError`. Hỗ trợ lưu lỗi validation trước khi mở transaction sách. Refs: FR-F4-19..23. |
| **T-F4-05** | Service Layer cho Book/BookCopy và Inventory Sync | `BookService.java` | 5h | T-F4-03, T-F4-04 | Mở transaction chung, truyền cùng Connection vào DAO, rollback khi lỗi. Chặn sửa ISBN/Barcode/số lượng tồn kho. Chỉ đổi condition BookCopy nếu `status='available'`. Ghi Audit Log. Refs: FR-F4-04A, FR-F4-07..09. |
| **T-F4-06** | Validator đọc Excel import | `BookImportValidator.java`, `BookImportWorkbookReader.java` | 5h | T-F4-02 | Chỉ nhận `.xlsx`, bắt buộc sheet `Books`/`BookCopies`, đúng cột, bỏ dòng trống, tách Category/Tag bằng `;`, giới hạn 5.000 BookCopy, phát hiện duplicate nội bộ và duplicate DB. Refs: FR-F4-10..12, FR-F4-21, FR-F4-24..28. |
| **T-F4-07** | Service import hàng loạt all-or-nothing | `BookImportService.java` | 6h | T-F4-03, T-F4-04, T-F4-06 | Nếu lỗi validation: không lưu Book/BookCopy, lưu batch+errors. Nếu hợp lệ: transaction tạo Book mới, tự tạo Category/Tag, BookCopy mặc định good/available, cập nhật số lượng, ghi Audit tổng hợp và chi tiết. Refs: FR-F4-13..14B, FR-F4-22. |
| **T-F4-08** | Service đối chiếu tồn kho cho Librarian | `InventoryReconciliationService.java`, `InventoryDAO.java` | 3h | T-F4-03 | Tính lệch kho trực tiếp từ DB khi mở/làm mới báo cáo. Không tự sửa `totalQuantity`/`availableQuantity`. Refs: FR-F4-15..18A. |
| **T-F4-09** | Controller Book và BookCopy | `BookServlet.java`, `BookCopyServlet.java` | 4h | T-F4-05 | GET danh mục/chi tiết, POST tạo/cập nhật. RBAC: chỉ LIBRARIAN được truy cập/ghi; ADMIN, MANAGER và role khác 403. Flash message tiếng Việt. |
| **T-F4-10** | Controller import và preview lỗi | `BookImportServlet.java` | 4h | T-F4-07 | Upload `.xlsx`, validate, hiển thị preview lỗi theo sheet/dòng/cột, confirm import hợp lệ, chặn ADMIN/MANAGER/role khác với HTTP 403. |
| **T-F4-11** | Controller đối chiếu tồn kho | `InventoryReconciliationServlet.java` | 3h | T-F4-08 | Màn hình kiểm kê/đối chiếu cho Librarian: danh sách phiên, chi tiết phiên, quét mã vạch, xử lý lệch kho. |
| **T-F4-12** | JSP quản lý sách cho Librarian | `web/librarian/book-*.jsp`, fragments liên quan | 5h | T-F4-09, T-F4-10 | 100% tiếng Việt, JSTL/EL, không scriptlet. Chỉ phục vụ LIBRARIAN; form không cho sửa ISBN/Barcode/số lượng tồn kho. Hiển thị flash message và lỗi import theo dòng. |
| **T-F4-13** | JSP đối chiếu kho cho Librarian | `web/librarian/book-inventory-reconciliation.jsp`, fragments liên quan | 4h | T-F4-11 | UI kiểm kê/đối chiếu cho Librarian; ADMIN/MANAGER không được mở route F4. |
| **T-F4-14** | RBAC và route F4 | `AuthFilter.java`, servlet mappings | 3h | T-F4-09, T-F4-10, T-F4-11 | Chỉ LIBRARIAN được truy cập `/book-management/*`; ADMIN, MANAGER và role khác HTTP 403. Kiểm tra ở server-side, không chỉ UI. |
| **T-F4-15** | Unit test DAO/Service/Validator | `test/dao/*`, `test/service/*`, `test/validator/*` | 6h | T-F4-05, T-F4-06, T-F4-07, T-F4-08 | Happy path và lỗi: duplicate ISBN/Barcode, rollback, chặn sửa định danh/số lượng, ADMIN/MANAGER 403, dòng trống, duplicate nội bộ, quá 5.000 BookCopy. |
| **T-F4-16** | Integration test import và tồn kho | `test/integration/*` | 5h | T-F4-07, T-F4-14 | Import hợp lệ tạo đúng Book/BookCopy/Category/Tag/Audit; import lỗi không thay đổi DB; cảnh báo lệch kho tính đúng. |
| **T-F4-17** | Performance smoke test danh mục và import | `test/performance/*` hoặc kịch bản thủ công có log | 2h | T-F4-12, T-F4-13 | Danh mục có filter P95 < 500ms; validate/import file gần 5.000 BookCopy trong ngưỡng chấp nhận cho Milestone 2. |
