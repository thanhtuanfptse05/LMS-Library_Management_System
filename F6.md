# Feature Specification: Quản lý Luân chuyển tại quầy (Desk Circulation)
# Version: 2.1.0 | Chủ sở hữu: @tech-lead | Ngày khởi tạo: 2026-06-14

## 1. Context & Goal (Ngữ cảnh & Mục tiêu)
Số hóa toàn bộ quá trình giao nhận vật lý và thu tiền tại quầy của thư viện, tập trung vào một Trạm trung chuyển (Dashboard) duy nhất.
Thay vì thủ thư phải thao tác rời rạc trên nhiều màn hình khác nhau, quy trình bắt buộc phải đi qua Hub tra cứu định danh một chạm qua mã vật lý.
Mục tiêu:
- Quản lý tập trung toàn bộ trạng thái Độc giả (đơn chờ lấy sách, sách đang mượn, nợ phạt) ngay trên một màn hình.
- Đảm bảo tính toàn vẹn dữ liệu kho và các luật nghiệp vụ (Business Rules) khắt khe: Không cho mượn nếu nợ phạt, Mượn trực tiếp tự sinh bản ghi chờ (chuẩn hóa), Kiểm kê thực tế hao mòn sách khi trả.
- Đơn giản hóa quá trình thanh toán và tự động mở khóa (Auto-unlock) tài khoản an toàn dựa trên cơ chế `UserLockReason`.

## 2. Actors & Roles (Tác nhân & Quyền hạn)
* **Librarian (Thủ thư):** Tác nhân cốt lõi duy nhất có thẩm quyền thực hiện các nghiệp vụ F6. 
  - Quyền tra cứu mã sinh viên/giảng viên (studentCode/lecturerCode).
  - Quyền quét mã vạch (barcode) để xuất/nhập kho.
  - Quyền xác nhận thanh toán tiền mặt và ghi nhận hao mòn tài sản.

## 3. Functional Requirements (Yêu cầu chức năng chi tiết)
*Biểu diễn dưới dạng EARS Notation.*

### Luồng 1: Tra cứu & Hub Trung chuyển (Desk Dashboard)
* **[FR-F6-01.01]:** WHEN Thủ thư nhập `studentCode` hoặc `lecturerCode` vào ô tìm kiếm tại Hub, THE system SHALL truy vấn hệ thống (giao cắt bảng `Student` và `Lecturer`) để ánh xạ ra `userId` tương ứng.
* **[FR-F6-01.02]:** WHERE mã định danh không tồn tại hoặc không hợp lệ, THE system SHALL hiển thị thông báo lỗi "Không tìm thấy độc giả với mã định danh này" và làm trống dữ liệu hiển thị.
* **[FR-F6-01.03]:** WHERE mã định danh hợp lệ, THE system SHALL truy vấn và hiển thị trên Dashboard 3 danh sách phân biệt: 
  1) Danh sách đơn chờ lấy sách (Reservation có `status = 'readypickup'`).
  2) Danh sách sách đang mượn cần trả (BorrowRecord có `status = 'borrowed'`).
  3) Danh sách các khoản nợ phạt hiện tại (Fine có `status = 'unpaid'`).
* **[FR-F6-01.04]:** WHILE Dashboard đang hiển thị thông tin độc giả, THE system SHALL kiểm tra trạng thái khóa của tài khoản (`User.status`) và hiển thị cảnh báo đỏ nổi bật nếu tài khoản đang bị khóa (`locked`), kèm liệt kê nguyên nhân từ `UserLockReason`.

### Luồng 2: Giao Sách (Check-out & Mượn trực tiếp)
* **[FR-F6-02.01]:** WHEN Thủ thư quét mã vạch (`barcode`) để giao/mượn sách, THE system SHALL truy vấn bảng `UserLockReason`. 
* **[FR-F6-02.02]:** WHERE tồn tại ít nhất một bản ghi có `reason == 'unpaid'`, THE system SHALL từ chối giao dịch, chặn mượn sách và hiển thị lỗi "Tài khoản đang nợ phạt, không thể mượn sách" (Tuân thủ BR-22).
* **[FR-F6-02.03] (Mượn trực tiếp):** WHEN Độc giả mượn trực tiếp tại quầy VÀ cuốn sách đó không có trong đơn chờ lấy của họ, THE system SHALL kiểm tra bảng `Reservation` của tựa sách đó.
  * WHERE đang có người chờ (`queuePosition > 0`), THE system SHALL từ chối cấp phát, rollback giao dịch và hiển thị "Sách đã được người khác đặt trước trong hàng đợi" (Tuân thủ BR-23).
  * WHERE hàng đợi trống, THE system SHALL tự động thực thi lệnh INSERT một bản ghi vào bảng `Reservation` (gán `queuePosition = 0`, `status = 'readypickup'`, `userId` hiện tại) ngay tại thời điểm mượn để chuẩn hóa nguồn dữ liệu giao dịch.
* **[FR-F6-02.04]:** WHERE các điều kiện mượn hợp lệ, THE system SHALL thực thi một DB Transaction (Atomic) duy nhất gồm: 
  - INSERT `BorrowRecord` (`status = 'borrowed'`, `startDate = GETDATE()`, `endDate` tính theo cấu hình role).
  - UPDATE `Reservation` (`status = 'fulfilled'`).
  - UPDATE `BookCopy` (`status = 'borrowed'`).

### Luồng 3: Nhận Sách (Check-in)
* **[FR-F6-03.01]:** WHEN Thủ thư quét mã trả sách, THE system SHALL yêu cầu Thủ thư xác nhận tình trạng vật lý của sách (Condition: 'good', 'damaged', 'lost').
* **[FR-F6-03.02]:** WHEN Thủ thư nhận trả sách với Condition IN ('damaged', 'lost'), THE system SHALL thực thi DB Transaction: 
  - UPDATE `Book.totalQuantity = totalQuantity - 1` (Tuân thủ BR-24).
  - UPDATE `BookCopy.status = 'unavailable'`.
  - UPDATE `BorrowRecord.status = 'returned'` (hoặc `lost`).
  - INSERT `Fine` phạt đền bù tài sản.
  - ĐỒNG THỜI INSERT `UserLockReason` (`reason='unpaid'`) VÀ UPDATE `User.status = 'locked'` ngay lập tức.
* **[FR-F6-03.03]:** WHEN Thủ thư trả sách với Condition == 'good', THE system SHALL UPDATE `BorrowRecord.status = 'returned'` VÀ `BookCopy.condition = 'good'`.
* **[FR-F6-03.04]:** WHILE Check-in Condition == 'good', THE system SHALL tìm bản ghi `Reservation` của tựa sách này đang đứng đầu hàng chờ (`queuePosition = 1`).
  * WHERE có người chờ, THE system SHALL UPDATE `Reservation` (`queuePosition=0`, `status='readypickup'`, gán `bookCopyId`) VÀ trigger Async Task gửi Email báo nhận sách.
  * WHERE không có người chờ, THE system SHALL UPDATE `Book.availableQuantity = availableQuantity + 1` VÀ `BookCopy.status = 'available'`.

### Luồng 4: Thanh toán Tiền mặt (Cash Payment & Auto-unlock)
* **[FR-F6-04.01]:** WHEN Thủ thư nhấn "Xác nhận thu tiền" cho một khoản nợ phạt (Payment), THE system SHALL thực thi DB Transaction: 
  - INSERT/UPDATE `Payment` (`status = 'completed'`, `paymentMethod = 'cash'`, `processedBy = currentUserId`).
  - UPDATE `Fine` (`status = 'paid'`).
  - DELETE bản ghi tương ứng có `reason = 'unpaid'` trong bảng `UserLockReason`.
* **[FR-F6-04.02] (Auto-unlock):** WHILE giao dịch thanh toán hoàn tất, THE system SHALL đếm (COUNT) số bản ghi còn lại trong `UserLockReason` của User.
  * WHERE COUNT == 0, THE system SHALL UPDATE `User.status = 'active'` (Gỡ khóa tài khoản).
  * WHERE COUNT > 0, THE system SHALL giữ nguyên trạng thái khóa `User.status = 'locked'` (Tuân thủ BR-25, BR-30).

## 4. Non-functional Requirements (Yêu cầu phi chức năng)
* **Transaction Concurrency (Locking):** Luồng kiểm tra hàng chờ Mượn trực tiếp (FR-F6-02.03) và Đẩy hàng chờ Check-in (FR-F6-03.04) BẮT BUỘC sử dụng Database Transaction Lock (`conn.setTransactionIsolation(Connection.TRANSACTION_SERIALIZABLE)` hoặc `SELECT ... WITH (UPDLOCK)`) để ngăn ngừa Race Condition khi 2 cuốn sách cùng tựa được xử lý cùng 1 mili-giây.
* **Toàn vẹn dữ liệu (Data Integrity):** Mọi thao tác Write ở mục 3 BẮT BUỘC phải nằm trong một SQL Transaction duy nhất (`conn.setAutoCommit(false)`). Nếu bất kỳ bước nào lỗi, phải Rollback toàn bộ trạng thái về ban đầu.
* **Bảo mật SQL:** 100% câu truy vấn phải dùng `PreparedStatement` kết hợp parameterize để chống SQL Injection.
* **Ghi nhật ký (Audit Log):** Bắt buộc lưu log kiểm toán vào `AuditLogs` khi xác nhận thanh toán thành công hoặc khi ghi nhận mất/hư hỏng sách (hành động thay đổi tài sản).

## 5. Database Schema & Data Models (Lược đồ dữ liệu)
Các bảng tương tác chính trong quá trình luân chuyển:
* **`[User]`, `Student`, `Lecturer`**: Dùng để JOIN và ánh xạ tìm ra `userId` gốc từ mã thẻ.
* **`UserLockReason`**: Bảng lõi để quyết định trạng thái khóa độc lập.
  - `userId` (INT, FK)
  - `reason` (NVARCHAR(50)) - 'unpaid', 'adminban', 'securitybreach'
* **`Book`, `BookCopy`**: Quản lý số lượng tồn kho và trạng thái từng ấn bản.
* **`BorrowRecord`**: Lưu lịch sử mượn trả.
* **`Reservation`**: Quản lý hàng đợi lấy sách.
  - `queuePosition` (INT) - `0` là trạng thái ảo (Mượn trực tiếp) hoặc đã có sẵn để lấy (`readypickup`); `>0` là đang xếp hàng chờ.
* **`Fine`, `Payment`**: Quản lý nợ phạt và quá trình thanh toán tại quầy.

## 6. Error Handling (Xử lý lỗi ngoại lệ)
* **WHERE** `studentCode` hoặc `lecturerCode` không khớp bất kỳ bản ghi nào trong cơ sở dữ liệu, **THE system SHALL** trả về mã HTTP 404 hoặc hiển thị cảnh báo UI "Không tìm thấy người dùng với mã định danh cung cấp".
* **WHERE** Thủ thư cố tình quét một `barcode` không tồn tại trong hệ thống, **THE system SHALL** báo lỗi "Mã vạch sách không hợp lệ" và chặn hành động.
* **WHERE** Quét mượn trực tiếp một sách đã có `queuePosition > 0` (đang có người đợi), **THE system SHALL** báo lỗi "Sách này đang có người đặt trước trong hàng đợi" và từ chối mượn.
* **WHERE** Lỗi `DatabaseException` (như mất kết nối hoặc deadlock transaction) xảy ra giữa chừng, **THE system SHALL** rollback toàn bộ transaction, ghi lỗi vào server log (sử dụng Logger), và báo UI "Hệ thống bận, vui lòng thử lại sau".
* **WHERE** Thanh toán thành công nhưng tài khoản vẫn còn `UserLockReason` khác (như adminban), **THE system SHALL** hiện Toast Warning: "Thanh toán thành công. Tuy nhiên tài khoản chưa được mở khóa do vi phạm kỷ luật khác."

## 7. Acceptance Criteria (Tiêu chí nghiệm thu)
- [ ] [TC-01] Tra cứu thành công Sinh viên bằng `studentCode` và Giảng viên bằng `lecturerCode`, load đúng `userId`.
- [ ] [TC-02] Thử mượn sách cho một User đang có `reason = 'unpaid'` -> Hệ thống từ chối thành công.
- [ ] [TC-03] Mượn trực tiếp 1 cuốn sách KHÔNG có ai đợi -> Hệ thống tự INSERT 1 dòng `Reservation` ảo với `queuePosition = 0`, sau đó Update thành `fulfilled`.
- [ ] [TC-04] Mượn trực tiếp 1 cuốn sách ĐANG có người đợi -> Bị chặn và báo lỗi rõ ràng.
- [ ] [TC-05] Check-in sách với tình trạng 'damaged' -> Sách chuyển 'unavailable', `Book.totalQuantity` giảm 1, sinh Fine, sinh UserLockReason, `User.status` thành 'locked'.
- [ ] [TC-06] Check-in sách với tình trạng 'good' và CÓ người đợi -> Update Reservation người đó thành `queue=0`, gán `bookCopyId`, `availableQuantity` KHÔNG TĂNG (vì đã phân bổ ngay). Async Task gửi Email được gọi.
- [ ] [TC-07] Nhận thanh toán Cash -> Cập nhật Fine 'paid', Payment 'completed', xóa dòng 'unpaid' trong `UserLockReason`. Nếu `COUNT=0` thì `User.status='active'`.
- [ ] [TC-08] Giả lập Race Condition khi 2 Thủ thư cùng mượn trực tiếp 1 tựa sách có 1 bản sao duy nhất -> Nhờ Locking, chỉ 1 giao dịch thành công, 1 giao dịch bị chặn do hết sách.

## 8. Out of Scope (Phạm vi không thực hiện)
* Không hỗ trợ tra cứu người dùng tại quầy bằng Tên (FullName) hoặc Số điện thoại. Chỉ sử dụng mã định danh cứng.
* Không hỗ trợ thanh toán phạt một phần (Partial Payment). Mọi giao dịch tại quầy mặc định là thu đủ 100% tiền phạt của khoản đó.
* Không xử lý cổng thanh toán trực tuyến (VNPAY) trong Module quản lý tại quầy này (thuộc module khác).
* Hệ thống in hóa đơn giấy (Receipt Printer) không nằm trong scope của v2.1.0.